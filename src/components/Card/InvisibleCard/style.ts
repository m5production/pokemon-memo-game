import styled from 'styled-components';
import { StyledCardBasic } from '../style';

export const StyledIvisibleCard = styled(StyledCardBasic)`
  background-color: transparent;
  border-color: transparent;
`